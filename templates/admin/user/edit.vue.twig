{# @var datatable AppDatatableDatatableInterface #}
{% extends 'base.vue.twig' %}
{% set title = 'User' %}

{% block template %}
    {{ vue_store('breadcrumbs', [
        { 'text': 'Dashboard', 'href': path('dashboard') },
        { 'text': 'User', 'href': path('admin_user_index') },
        { 'text': user.username, 'href': path('admin_user_index') },
        { 'text': 'Edit' },
    ]) }}
    {{ vue_store('form', jsonForm) }}
    <v-form action="{{ path('admin_user_edit', user) }}" method="post">
    <FormType :form="$store.form" />
        <v-btn type="submit" color="primary" class="mt-5">Save</v-btn>
    </v-form>
{% endblock %}