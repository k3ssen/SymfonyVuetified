{# The 'fetch' header is added by the FetchComponent #}
{% set isFetchRequest = app.request.headers.get('fetch') %}
{% extends isFetchRequest ? 'layout/base-fetched-page.html.twig' : 'layout/base-full-page.html.twig' %}
